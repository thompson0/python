largura = float(input('Digite a largura do triangulo '))
base = float(input('Digite a base do tringulo '))

resultado = base * largura/2

print(f'O tringulo com base {base} e largura {largura} tem area igual {resultado}')
